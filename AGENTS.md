# AGENTS.md — Codex 小游戏工作室项目规则

## 1. 项目定位

本仓库用于在 Codex 协助下，从零完成一款可正式发布的移动小游戏，并形成一套后续可复制到其他小游戏的生产模板。

目标平台：
- 微信小游戏
- 抖音小游戏

首选技术栈：
- Cocos Creator 3.8.x（优先锁定一个具体 3.8.x 版本，不随意升级）
- TypeScript
- 微信开发者工具
- 抖音开发者工具
- Git / GitHub

项目目标不是只做出“能运行的 Demo”，而是完成完整闭环：

创意/需求 → GDD → 原型 → 核心玩法 → 正式内容 → UI/美术/音频 → 平台适配 → 广告/数据 → 测试 → 性能优化 → 资质/合规准备 → 双平台提审 → 发布 → 上线后数据验证

---

## 2. Codex 工作方式

### 2.1 总原则

1. 先理解，再修改；先检查现状，再提出实现方案。
2. 优先做最小可验证变化，不做无关重构。
3. 每个阶段都有明确的“完成定义（Definition of Done）”。
4. 不把猜测当事实；涉及平台规则、SDK、审核要求、引擎 API、当前版本限制时，优先查官方最新文档。
5. 对无法验证的结论明确标注“不确定”，不要编造。
6. 涉及账号、AppID、密钥、广告 ID、服务器密钥、用户数据等敏感信息时，不得写入 Git。
7. 不得为了“让测试通过”而伪造测试结果、删除真实失败、绕过平台限制或修改验收标准。
8. 不得擅自上传版本、提交审核、发布线上版本、删除远程资源或执行不可逆操作；这些动作必须得到用户明确授权。
9. 不主动提交 Git commit，除非用户明确要求。
10. 当需求存在关键产品决策时，先给出 2～4 个清晰选项与建议；不要在未经确认的情况下替用户决定核心玩法。

### 2.2 工作循环

每个任务尽量遵循：

1. 读取相关文档与现有代码。
2. 判断当前项目阶段。
3. 列出本次任务的目标、非目标和验收标准。
4. 实施最小修改。
5. 运行适当的检查/构建/测试。
6. 对 UI 或视觉变化尽可能通过截图、真机或实际预览验证。
7. 总结：改了什么、如何验证、还有什么风险。

### 2.3 不要重复前置工作

用户可以提前完成以下工作：
- Cocos Creator 安装
- Node.js 安装
- Git 安装
- VS Code 安装
- 微信开发者工具安装
- 抖音开发者工具安装
- 微信开发者账号注册
- 抖音开放平台账号注册
- 微信小游戏 AppID 获取
- 抖音小游戏 AppID 获取

这些属于“环境与账号前置条件”。

如果已经存在，Codex 只做必要的验证，不要无故重新安装、重新注册或修改账号设置。

如果缺失，先明确告诉用户缺失项，并优先提供最短补齐路径；不要在没有授权时进行账号注册、认证、支付或发布操作。

---

## 3. 项目阶段模型

项目必须按阶段推进，不允许一开始就无限堆功能。

### Phase 0 — 环境确认

目标：确认现有开发环境可用于本项目。

需要验证：
- Cocos Creator 版本
- Node.js 版本
- Git 可用
- VS Code 可用
- 微信开发者工具可用
- 抖音开发者工具可用
- Git 仓库状态正常

完成定义：能够打开/创建 Cocos Creator 项目，并完成最基础的运行或构建验证。

如果环境已经由用户准备完成，不重复安装。

### Phase 1 — 产品定义

输出：
- `docs/product-brief.md`
- `docs/gdd.md`
- `docs/roadmap.md`

至少明确：
- 游戏名称（暂定名也可以）
- 类型
- 目标平台
- 横屏/竖屏
- 一局时长
- 核心玩法
- 核心循环
- 核心用户体验
- 广告模式
- 首版范围
- 明确不做的内容

第一款游戏默认以“小而完整”为原则：优先 2D、轻量、单机或弱联网、短局、低后端依赖。

### Phase 2 — 技术设计

输出：
- `docs/technical-design.md`
- 目录结构说明
- 平台适配策略
- 数据结构说明
- 存档策略
- 广告接口设计
- 日志与数据统计策略
- Asset Bundle / 分包策略

原则：微信和抖音共享游戏核心逻辑，平台差异集中在适配层。

建议结构：

```text
assets/
  scenes/
  prefabs/
  textures/
  audio/
  animations/
  resources/
  scripts/
    core/
    gameplay/
    ui/
    data/
    platform/
    services/
```

不要在业务代码中到处直接散落 `wx.*` 或 `tt.*` 调用；优先通过平台适配层统一封装。

### Phase 3 — 可玩原型

目标：尽快得到一个“不漂亮但真的好玩/可验证”的版本。

优先完成：
- 启动
- 开始游戏
- 玩家输入
- 核心玩法
- 胜负/失败
- 得分或结果
- 重新开始

原型阶段暂时不追求正式美术、复杂经济、复杂剧情和完整广告系统。

完成定义：无需额外说明，首次进入后可以完整玩完一局并重新开始。

### Phase 4 — 正式游戏内容

逐步加入：
- 正式 UI
- 正式角色/物体
- 场景
- 动效
- 音频
- 数值
- 关卡/内容
- 教学
- 存档
- 设置
- 错误处理

所有容易调平衡的关键数字尽量配置化，不要散落硬编码。

### Phase 5 — 平台能力

微信和抖音分别处理：
- 启动
- 生命周期
- 广告
- 分享
- 振动
- 登录/用户标识（确实需要才做）
- 存储
- 网络
- 平台特有 API

任何平台 API 使用前：
1. 确认当前目标平台。
2. 确认当前 SDK/工具版本。
3. 查官方文档。
4. 保留最小兼容代码。

不要因为网上旧文章使用过某个 API 就直接照搬。

### Phase 6 — 素材生产

素材采用：
- AI 生成辅助
- 人工整理/修图
- 程序化组合

不要直接把原始 AI 输出全部放进游戏。

建立统一视觉规范：
- 角色风格
- 配色
- 线条/细节密度
- 光照
- UI 风格
- 图标风格

素材命名使用稳定、可检索的英文命名，避免使用“最终版/最终版2/真正最终版”等不可维护命名。

例如：

```text
player_idle_01.png
player_run_01.png
enemy_slime.png
coin_gold.png
btn_start.png
bg_game_01.png
```

注意素材版权：
- 不得使用无法确认授权的商业素材。
- 不得故意仿制在世商业 IP 的角色、Logo、UI 或美术风格以规避版权风险。
- AI 生成素材需要保留来源/生成记录，商业使用前确认对应服务条款和授权范围。

### Phase 7 — QA / 测试

测试必须分层：

1. 编辑器运行
2. 微信开发者工具模拟器
3. 微信真机
4. 抖音开发者工具模拟器
5. 抖音真机
6. 不同性能等级设备
7. 网络异常
8. 广告异常
9. 生命周期异常
10. 首次安装/二次启动/升级场景

测试至少覆盖：
- 首次启动
- 新手流程
- 开始/暂停/继续
- 核心玩法
- 胜负
- 重试
- 存档
- 广告成功/失败/取消
- 网络断开/恢复
- 后台切换/恢复
- 退出再进入
- 设备旋转/不同分辨率（如适用）
- 低性能设备
- 长时间运行

任何“理论上没问题”的结论，如果实际运行可以验证，应尽量实际验证。

### Phase 8 — 性能与包体

重点关注：
- 首次启动时间
- 首屏资源
- CPU/GPU 占用
- 内存
- Draw Call
- 纹理尺寸
- 图集
- 音频大小
- Asset Bundle
- 分包
- 网络请求数量
- 峰值内存

原则：
- 不为方便而无脑使用超大纹理。
- 不把所有资源塞入主包。
- 不在首屏加载非必要资源。
- 不为了性能优化而提前引入复杂框架，先测量再优化。

涉及具体平台包体限制时，以当前平台官方文档为准，不依赖 AGENTS.md 中的历史数字。

### Phase 9 — 合规 / 资质 / 上架准备

此阶段要建立发布清单：

- 游戏名称一致性
- 主体信息
- 软件著作权
- 相关备案/资质
- 隐私政策
- 用户协议（如适用）
- 实名/防沉迷相关要求（如适用）
- 广告合规
- 游戏内容合规
- 网络域名/安全域名（如适用）
- 商店图标
- 游戏截图
- 游戏介绍
- 宣传视频
- 版本号
- 发布说明

涉及中国大陆游戏发行政策、平台审核标准、备案、版号、实名认证、防沉迷等内容时，必须查当前官方来源，并把“必须做”“视游戏情况而定”“暂不需要”区分清楚。

不要把“小游戏备案”和“版号/出版审批”等不同概念混为一谈。

### Phase 10 — 双平台提审

提审前必须形成 Release Candidate（候选发布版本）。

候选版本必须：
- Git 工作区状态可追踪
- 版本号确定
- 构建参数确定
- 核心流程通过
- 真机通过
- 广告通过
- 存档通过
- 平台能力通过
- 素材和文字资料检查完成
- 发布包可以复现

不要直接拿“开发中版本”提交审核。

### Phase 11 — 上线后验证

上线后优先观察：
- 启动成功率
- 首局开始率
- 首局完成率
- 平均游戏时长
- D1/D3/D7 留存（可取得时）
- 激励广告展示率
- 激励广告完成率
- 单用户广告收入
- 崩溃/报错
- 关键关卡流失

先发现问题，再迭代，不要因为一天收入低就立即大改核心玩法。

---

## 4. Git / GitHub 规则

### 必须纳入版本控制

- `assets/`
- `settings/` 中项目需要的配置
- `package.json`
- `tsconfig.json`
- `README.md`
- `AGENTS.md`
- `docs/`
- 重要脚本与工具配置

### 默认不要提交

- 本地构建输出
- 临时缓存
- IDE 私有缓存
- 平台本地登录缓存
- 私钥
- API Key
- App Secret
- 广告密钥
- 个人账号信息
- 包含密码/令牌的配置文件

使用 `.env.example` 或安全占位符说明配置方式。

### Commit 原则

只有在用户明确要求时才 commit。

用户要求 commit 时：
- commit 信息简洁、描述行为
- 不把多个无关修改混在一个 commit
- 提交前运行与本次变更相关的验证

---

## 5. Cocos Creator 特别规则

1. 默认使用固定的 Cocos Creator 3.8.x 版本。
2. 涉及引擎 API 时优先查看对应版本官方文档。
3. 不无故升级 Cocos 版本。
4. 不无故重建现有 Canvas、Scene、Prefab。
5. 修改 Scene/Prefab 时尽量做最小变更。
6. 不随意修改已有资源 UUID 或破坏 `.meta` 关联。
7. 不直接编辑 Cocos 生成物，除非任务明确要求且知道后果。
8. 代码、场景、Prefab、数据之间的职责必须清楚。
9. GameManager 不应成为所有逻辑的垃圾桶；新增系统优先使用独立模块。
10. 可以进入 Inspector 配置的可调参数，优先数据化/配置化。

---

## 6. TypeScript 规则

- 类名使用 PascalCase。
- 方法、变量使用 camelCase。
- 常量使用明确、稳定的命名。
- 避免超长方法。
- 一个类尽量保持单一职责。
- 公共接口必须有清晰命名和必要的类型。
- 不使用 `any` 隐藏真实类型问题，除非有明确理由并局部隔离。
- 不复制粘贴相同业务逻辑；优先提取共享服务。
- 相对导入遵循当前 Cocos 项目的既有约定，不自行引入冲突风格。

---

## 7. 数据与配置规则

以下内容优先配置化：
- 关卡数据
- 敌人数值
- 玩家属性
- 道具属性
- 奖励
- 商店价格
- 广告奖励
- 冷却时间
- 难度参数
- 文案

建议：

```text
assets/resources/data/
```

保存结构化数据。

玩法公式和规则必须集中管理，避免多个脚本分别实现“同一公式”。

---

## 8. 广告与商业化规则

第一版优先使用简单、自然的广告场景：
- 看激励广告复活
- 看激励广告获得额外奖励
- 看激励广告领取一次性奖励

禁止为了“增加广告收入”而破坏核心体验。

所有广告逻辑必须考虑：
- 广告加载失败
- 用户取消
- 广告未完整观看
- 平台回调异常
- 重复领取
- 断网
- 切后台

奖励必须具备幂等性，避免一次广告产生多次奖励。

具体广告 SDK、参数、平台 API 以当前官方文档和实际接入方案为准。

---

## 9. 日志、错误与调试

开发期日志应帮助回答：
- 当前状态是什么
- 调用了什么平台能力
- 回调是否成功
- 数据是否正确
- 为什么没有发放奖励

正式版本不得打印敏感信息。

不要把 App Secret、Token、用户完整身份信息或其他敏感数据写入日志。

---

## 10. 文档规则

推荐至少维护：

```text
README.md
AGENTS.md

 docs/
   product-brief.md
   gdd.md
   roadmap.md
   technical-design.md
   qa-checklist.md
   release-checklist.md
   changelog.md
   decisions/
```

重大技术或产品决策写入 `docs/decisions/`，而不是只存在聊天记录里。

开发过程的重要踩坑应沉淀到文档，让后续 Codex 会话不必重复踩坑。

---

## 11. Codex 与阶段任务的关系

AGENTS.md 是“长期规则”，不是一次性任务清单。

具体开发任务应记录在：
- `docs/roadmap.md`
- GitHub Issues
- 必要时的单独 task/brief 文档

不要把所有未来工作都塞进 AGENTS.md。

当一个阶段完成后，更新项目状态文档，而不是不断把历史过程追加到 AGENTS.md。

---

## 12. 外部资料与当前事实

以下内容必须优先使用官方资料验证：
- Cocos Creator 当前版本 API
- 微信小游戏开发规范
- 微信开发者工具行为
- 抖音小游戏开发规范
- 抖音开发者工具行为
- 广告 SDK/API
- 包体限制
- 审核规则
- 备案/资质
- 实名/防沉迷
- 隐私与数据政策

推荐来源优先级：

1. 官方文档
2. 官方 GitHub / 官方示例
3. 官方公告
4. 高质量社区资料
5. 普通博客/帖子

发现第三方资料与官方资料冲突时，以当前官方资料为准。

---

## 13. 发布安全门槛

以下动作属于发布级动作，必须明确得到用户授权：

- 上传微信版本
- 提交微信审核
- 发布微信正式版本
- 上传抖音版本
- 提交抖音审核
- 灰度/全量发布
- 修改线上广告配置
- 修改线上后端配置
- 删除线上数据
- 删除云资源

在获得授权前，可以：
- 本地开发
- 本地构建
- 本地模拟
- 真机测试
- 生成提交所需的文件和检查报告

但不要自行执行发布。

---

## 14. 每次完成任务后的报告格式

完成一个任务后，用下面的结构简要汇报：

### Done
- 完成了什么

### Changed
- 修改了哪些文件

### Verified
- 执行了哪些检查/测试
- 测试结果

### Remaining
- 尚未完成的事项

### Risks
- 已知风险、平台依赖或待人工确认事项

如果任务未完成，不要说“已完成”。

---

## 15. 当前项目状态占位

以下信息由用户后续补充，Codex 不得自行编造：

- 游戏名称：待确定
- 游戏类型：待确定
- 游戏方向：待确定
- Cocos Creator 精确版本：待确认
- 微信 AppID：不写入仓库
- 抖音 AppID：不写入仓库
- 开发主体：待确认
- 软件著作权：待办理/待确认
- 备案/资质状态：待确认
- 广告方案：待确定

---

## 16. 第一款游戏的核心原则

这是第一款游戏，不是最终商业大作。

优先级：

1. 能完成
2. 能玩
3. 能稳定运行
4. 能在微信和抖音运行
5. 能通过基本审核要求
6. 能上线
7. 能获得真实用户数据
8. 根据数据迭代

不要因为追求“完美架构”而推迟第一款游戏上线。

但不要为了赶进度破坏后续复用能力。核心平台层、广告层、存档层、配置层和基础 UI 能够复用的部分，应适度抽象。

---

## 17. 与用户协作的原则

用户是最终产品决策者。

Codex 负责：
- 分析
- 设计建议
- 编码
- 调试
- 测试
- 文档
- 构建
- 技术风险提示

用户负责最终决定：
- 游戏创意
- 核心玩法
- 美术方向
- 商业化取舍
- 是否提交审核
- 是否发布

涉及核心产品方向时不要擅自改变产品目标。

---

## 18. 最终成功标准

这个项目真正完成的定义不是“代码写完”，而是：

- 第一款小游戏有明确版本号
- 微信版本完成实际可玩验证
- 抖音版本完成实际可玩验证
- 关键流程稳定
- 性能和包体达到可发布标准
- 必要资料/资质已准备
- 审核所需材料齐全
- 经用户授权完成平台提交
- 正式上线或明确完成发布前最后一步
- 上线后的数据指标能够被记录并用于下一轮迭代

第一款游戏完成后，把通用能力沉淀成模板，为第二款、第三款小游戏复用。
